<h1><%= @list.name %></h1>
<p><%= @list.movies.size %>"movie"</p>
<%= link_to "add a movie", new_list_bookmark_path(@list), class: 'btn btn-flat' %>
<% @list.movies.each do |movie| %>
  <%= image_tag movie.poster_url %>
<% end %>
